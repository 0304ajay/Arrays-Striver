#include<vector>

int getSingleElement(vector<int> &arr){
	// Write your code here.	
	int i=0;
	int n=arr.size();
	for(i=0;i<n;++(++i))
	{
		if((i+1==n)||arr[i]!=arr[i+1])
		{
			return arr[i];
		}
	}
}
